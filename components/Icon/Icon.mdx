---
name: Icon
route: /components/icon
---

# Icon

import { Playground } from 'docz';
import icons from '../../config/icons';
import Icon from './';

<Playground>
  <div>
    {icons.map((icon, index) => (
      <Icon name={icon.name} marginRight="s" />
    ))}
  </div>
</Playground>
